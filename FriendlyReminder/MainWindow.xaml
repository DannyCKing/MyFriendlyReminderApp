<Window x:Class="FriendlyReminder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FriendlyReminder"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="Friendly Reminder" Height="450" Width="600"
        Background="#F4F7FB"
        >
    <Grid>
        <Grid Width="500" Height="400" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Button Style (reusable) -->
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="10" />
                    <Setter Property="FontSize" Value="20" />
                    <Setter Property="Padding" Value="12" />
                    <Setter Property="Background" Value="#4A90E2"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Height" Value="80"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="10" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Button.BorderBrush" Value="#4A90E2"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3A7BCD"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#2F66A3"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>

            <TextBlock x:Name="ReminderTitle"
                   Text="Friendly Reminder"
                      TextAlignment="Center"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="#333"
                   Margin="0 20 0 10"
                   TextWrapping="Wrap" />
            
            <!-- Buttons -->
            <Button x:Name="NewReminderButton" Grid.Row="1" Content="Create New Reminder" Click="NewReminderButton_Click" />
            
            <!-- to do: show other buttons-->
            <!--<Button x:Name="CreateReminderFromTemplateButton" Grid.Row="2" Content="Create Reminder From Template" Click="CreateReminderFromTemplateButton_Click" />
            <Button x:Name="CreateTemplateButton" Grid.Row="3" Content="View Templates" Click="CreateTemplateButton_Click" />
            <Button x:Name="CurrentRemindersButon" Grid.Row="4" Content="See Current Reminders" Click="CurrentRemindersButon_Click" />-->


            <Button Grid.Row="5"
                    Content="Hide"
                    Background="#D9534F" Click="Button_Click">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C64541"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#A53734"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
